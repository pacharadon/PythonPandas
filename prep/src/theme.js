// Shared visual constants. Edit here to re-skin the whole app.
//
// v4.4 — Light theme · Pantone-inspired blue ramp on white.
//
// Surfaces:    white → very-pale-blue → pale-blue
// Accent:      deep-blue → medium-blue → light-blue
// Text:        near-black-blue → slate gradations

// Surfaces (light → bluer)
export const bg        = '#ffffff'; // page background — white
export const surface   = '#f5f8ff'; // cards, sidebar — Pantone 663 vibe
export const surface2  = '#e7eefc'; // nested cards, hover — Pantone 656 vibe
export const codeBg    = '#f0f4fb'; // textareas, pre blocks — very pale
export const border    = '#cfdbef'; // dividers — soft blue-gray

// Accent ramp (deep → light, all blue)
export const accent      = '#1e40af'; // deep blue — Pantone 2738 C · primary CTAs
export const accentDim   = '#3b82f6'; // medium blue — Pantone 285 C · highlights
export const accentLight = '#93c5fd'; // light blue — Pantone 284 C · soft accents

// Text on light surfaces
export const text        = '#0f172a'; // slate-900 — primary body
export const textMuted   = '#475569'; // slate-600 — secondary body
export const textSubtle  = '#64748b'; // slate-500 — labels
export const textFaint   = '#94a3b8'; // slate-400 — disabled / null cells

// Text on accent surfaces
export const textOnAccent = '#ffffff'; // white text on deep-blue buttons
